VP_USERNAME=
VP_PASSWORD=
VP_START_URL=http://54.214.7.161/
VP_LOG_LEVEL=INFO
# Optional: proxy URL (e.g. http://user:pass@host:port) used only to retry
# requests that come back geo-blocked. Leave unset to skip and flag them instead.
VP_REGION_PROXY=